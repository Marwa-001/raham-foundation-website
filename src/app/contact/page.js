import ContactClient from './ContactClient';

export const metadata = {
  title: 'Contact Us',
  description: 'Get in touch with Raham Foundation. Reach out to ask questions, request partnership details, seek media info, or query volunteer opportunities.',
};

export default function ContactPage() {
  return <ContactClient />;
}
